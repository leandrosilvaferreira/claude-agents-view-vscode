# Marketplace screenshot

`resources/screenshot.png` is an edited illustration for the README, generated with the built-in image generation tool from a reference screenshot. Project, session, branch and agent text was replaced with fictional English examples. The intent was to preserve the existing interface, not to demonstrate new functionality.

The image is not a pixel-identical capture: generated texture and antialiasing can differ. The README labels it as illustrative. Keep the reference screenshot and real workspace names out of published assets and documentation.

## Final sanitized editing prompt

```text
Use case: text-localization. Edit the screenshot for an extension-store README.

Preserve the original tree layout, row count, indentation, dark background,
typography, three original product logos, chevrons, status icons, robot icons,
eye and refresh icons, and UI chrome.

Replace every private project, session, branch and agent name and every
Portuguese description with generic English examples. No original private
strings may remain. Do not add panels or features.

Use these examples:
- storefront-app [fix/webhook-security] — Harden webhook validation
- customer-portal [feat/account-permissions] — Add account permissions
- developer-tools — Improve subagent visibility
- knowledge-service — Update tool bindings
- Google Antigravity Session a1b2c3d4 — Build a sample dashboard
- Google Antigravity Session e5f6a7b8 — Review component layout
- workflow-service [main] — Implement workflow validation
- manifest-validator and parser-reviewer — Update: Configuration validation is complete
- extension-toolkit [feat/session-monitor] — Add session monitoring
- qa-reviewer — Update: Documentation and tests are verified

Keep the brand labels Claude Code, Google Antigravity and Codex exact.
Keep Working Agents and Completed Agents exact.
```

## Validation

- Output: PNG, 1209 × 1301 pixels.
- README reference: `resources/screenshot.png`, with descriptive alternative text and an illustration caption.
- Review the full image at readable size before publishing: all example text should be English, all brand labels recognizable, and no private project names, paths or conversation text should remain.
- Editing this repository does not publish or install the extension. Asset and marketplace updates take effect only in a later authorized release.
